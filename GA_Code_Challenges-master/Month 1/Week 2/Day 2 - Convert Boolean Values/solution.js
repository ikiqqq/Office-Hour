function boolToWord( bool ){
    if (bool == true)
      return "Yes"
    else bool == false
          return "No"
  }

  //best solution

  function boolToWord( bool ){
    return bool ? 'Yes':'No';
  }

  const boolToWord = bool => bool ? 'Yes' : 'No';

  